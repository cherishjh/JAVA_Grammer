package C12ClassLecture;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class C1207RecurCombiPermu_practice2 {
    static boolean[] visited;

    public static void main(String[] args) {
    /*    int k=80;
        int [][] dungeons={{80,20},{50,40},{30,10}};

        List<List<Integer>> dungeon_list = new ArrayList<>(Arrays.asList());  // 1,2,3,4
        List<List<Integer>> combinations = new ArrayList<>();
        List<Integer> temp = new ArrayList<>();
        visited = new boolean[dungeon_list.size()];

        combination(k,);
        System.out.println(combinations);




    }

    static void combination(int k,    List<Integer> temp, List<List<Integer>> combinations, List<Integer> myList) {
        if (temp.size() == k) {
            return;
        }
        for (int i ; i < myList.size(); i++) {
            temp.add(myList.get(i));
            combination(i + 1, temp, n, combinations, myList);
            temp.remove((temp.size() - 1));
        }
    }

*/
    }
}